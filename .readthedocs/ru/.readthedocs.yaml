# Read the Docs configuration for the Russian translation project.
#
# Read the Docs requires the file to be named exactly ".readthedocs.yaml", so
# this Russian variant lives in a subdirectory. In the fast-excel-writer-ru
# project settings set the config path to:
#     .readthedocs/ru/.readthedocs.yaml
# and link that project as a translation of fast-excel-writer.
#
# All paths below are relative to the REPOSITORY ROOT (not this subdirectory),
# and build.jobs commands run from the repository root.
# See https://docs.readthedocs.io/en/stable/config-file/v2.html

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    # docs/ru holds only the translated pages. Copy the shared assets and the
    # English-only API reference pages (91..97) into it so the standalone
    # Russian site is self-contained. The 90-api-reference.md overview is
    # translated and must NOT be overwritten, hence the 9[1-7] glob.
    pre_build:
      - cp -r docs/img docs/ru/img
      - cp -r docs/ext docs/ru/ext
      - find docs -maxdepth 1 -type f -name '9[1-7]-api-*.md' -exec cp {} docs/ru/ \;

mkdocs:
  configuration: mkdocs.ru.yml

python:
  install:
    - requirements: docs/requirements.txt

formats:
  - pdf
